# Dependencies
node_modules/

# Build output
dist/
coverage/

# Reference-only source (read-only port-from material)
.example/

# Editor/OS
.DS_Store
*.log

# Claude project memory — local-only working notes (un-ignore if/when ready to share)
CLAUDE.md


# Local-only notes (not synced)
*.local.md

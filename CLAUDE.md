## Project knowledge

This repository contains a **Grafana panel plugin**. Read `./.config/AGENTS/instructions.md` before making changes — it's Grafana's official guidance for AI agents working on plugins.

If `./.example/CLAUDE.md` exists in your local checkout (gitignored, not in the public repo), read it too — it's the project-specific memory: scope, modes, working principles, and notes on the pre-refactor reference implementation under `./.example/`.
